export const IS_DEV = true;
export const BASE_API_URL = '/testing-api';

export const WEB_NAME = 'Vue Sneaker';

export const BREAKPOINTS = Object.freeze(["xs", "sm", "md", "lg", "xl", "xxl"]);
export const MOBILE_DEVICES = Object.freeze(BREAKPOINTS.slice(0, 2));

export const THEME_NAMES = Object.freeze(['light', 'dark', 'blue'] as const);

export const EMPTY_ARRAY = Object.freeze([] as any[]);